if (keyboard_check(ord("D"))) {
	move_contact_solid(0, stepSize);
}
if (keyboard_check(ord("W"))) {
	move_contact_solid(90, stepSize);
}
if (keyboard_check(ord("A"))) {
	move_contact_solid(180, stepSize);
}
if (keyboard_check(ord("S"))) {
	move_contact_solid(270, stepSize);
}
